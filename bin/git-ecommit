#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/scripts/core/_main.sh"

##? Git commit with emojis
#?? 1.0.0
##?
##? Usage:
##?    git-ecommit

docs::eval "$@"

git add .
gitmoji commit -S -c
