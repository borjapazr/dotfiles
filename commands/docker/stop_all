#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/commands/core/_main.sh"

##? Stop all docker containers
#?? 1.0.0
##?
##? Usage:
##?   stop_all
docs::parse "$@"

platform::require docker || exit 1

docker ps -a -q | xargs -I_ docker stop "_"
